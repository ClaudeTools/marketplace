---
description: "Research external APIs, libraries, and docs before implementing. Use before writing code that touches external services."
argument-hint: "[topic, API, or library]"
---

Invoke the `claudetools:research` skill with the user's arguments.
