---
description: "Evidence-based debugging — reproduce, observe, hypothesize, verify, fix. No guessing."
argument-hint: "[error description or symptom]"
---

Invoke the `claudetools:debugger` skill with the user's arguments.
After root cause is found, tell the user: "Root cause identified. **Next: /build** to implement the fix."
